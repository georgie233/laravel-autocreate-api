import {MODEL}List from './{MODEL}List.vue'
//可搜索的字段
const search = [
    {SEARCH_ARR}
];
//列表显示的字段
const columns = [
    {COLUMNS_ARR}
];
//要关联查询的数组
const withArr = [
    // {key:'with_category',get:'name'}
];
export default {MODEL}List
export{
    search,
    columns,
    withArr
}